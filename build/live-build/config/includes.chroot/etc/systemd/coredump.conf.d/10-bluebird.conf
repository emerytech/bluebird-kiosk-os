# BlueBird Kiosk OS — don't persist coredumps. A crashing chromium would otherwise drop
# multi-hundred-MB cores under /var/lib/systemd/coredump on a disk nobody prunes; the
# watchdog already restarts the session, so cores add cost without recovery value.
[Coredump]
Storage=none
ProcessSizeMax=0
